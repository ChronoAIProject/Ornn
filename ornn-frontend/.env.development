# Microservice URLs for development
# In dev mode, Vite proxy handles routing so these are not strictly required.
# For production builds (or when not using Vite proxy), set these to the service URLs.
VITE_AUTH_URL=http://localhost:3801
VITE_SKILL_URL=http://localhost:3802
VITE_PLAYGROUND_URL=http://localhost:3803
