---
---

docs(skills): split `ornn-agent-manual` into two transport-specific skills — `ornn-agent-manual-cli` (uses the NyxID CLI) and `ornn-agent-manual-http` (direct HTTPS with a NyxID bearer token). Each variant is transport-pure: no cross-references between the two, no leakage of CLI examples into the HTTP variant or vice versa. Both are published as system skills tied to the `ornn-api` NyxID service; the original `ornn-agent-manual` v2.3 is marked deprecated on the registry with a note pointing at the replacements. Restructured contents: §0.5 documents `~/.ornn/installed-skills.json` (cross-session install registry), §1 (Prerequisites) is transport-specific, §2 (Core Workflows) is reorganised into 13 self-contained use cases each linking to the matching `references/api-reference.md` section, §3 collapses redundant audit / anonymous bullets and adds frontmatter pitfalls (unquoted version, singular `tag` key). Repo source: `skills/ornn-agent-manual-cli/` and `skills/ornn-agent-manual-http/`.
