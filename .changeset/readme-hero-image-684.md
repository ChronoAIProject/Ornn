---
---

Add Ornn hero brand image to `README.md` (#684). Drops `ornn-web/public/hero-brand.svg` alongside the existing logo assets and inserts a centered hero block between the badges row and the status pills — `logo → badges → hero → pills → tagline → links → nav`. Docs-only, no code or schema change.
