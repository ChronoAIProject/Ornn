---
"ornn-web": patch
---

Install card — COPY button is now a small accent (orange) pill floating in the top-right corner of the code field. The code area takes the full width of the card, no sidebar.
