---
---

Install Vitest + Testing Library + jsdom for `ornn-web`. Existing bun:test-based tests under `ornn-web/src/lib/` migrate to vitest imports; both test files now run in CI via `bun run test`. No runtime changes.
