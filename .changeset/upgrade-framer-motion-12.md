---
"ornn-web": patch
---

Upgrade `framer-motion` 11 → 12. Retypes the three `Variants` definitions in the codebase (SkillGrid, ExplorePage, MySkillsPage) and the `PageTransition.ease` prop to match v12's tightened animation typing.
