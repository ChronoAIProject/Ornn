---
"ornn-web": patch
---

Install card — restore code field to full width, shorten the box from h-40 (160px) to h-32 (128px). Prompt previews ~5 lines before scroll without dominating the page vertically.
