---
---

Replace LoL-derived Ornn logo with an original wordmark — Geist Pixel Line on the Industrial Forge heated gradient. Font is inlined in the SVG so it renders reliably as `<img>` / favicon. `logo-alt.svg` (Triangle variant) kept as a backup. Drops the old `logo.png` / `ornn.png` / `ornn_hammer*.gif` assets.
