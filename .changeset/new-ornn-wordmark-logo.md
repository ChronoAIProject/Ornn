---
---

Ship the bronze 3D ORNN logo with smoothed edges. The previously committed wordmark placeholder is replaced with a high-fidelity trace of the canonical copper-bronze ORNN mark. Traced paths were refit as smooth Béziers (1008 jagged polygons → 643 smooth spline paths) and minified (-35%), so the mark renders sharply at every scale without pixel-step aliasing. `logo-alt.svg` (Geist Pixel wordmark variant) retained as a backup.
