---
---

Empty changeset — meta-sync only. Re-applies the v0.5.0 release version bump onto develop after the stale sync PR (#236) was closed. No code or feature change; ships only the `package.json` version bumps to 0.5.0 and appends the 0.5.0 sections to both `CHANGELOG.md` files. See #328.
