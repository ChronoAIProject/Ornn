---
---

Add `chrono-ai-service-manual` skill — unified Ornn agent manual that folds the legacy `ornn-agent-manual-cli` + `ornn-agent-manual-http` content together with the NyxID identity / CLI / proxy / token-model context. Single skill, two halves (NyxID + Ornn), shared identity bootstrap, single failure-mode appendix. Legacy manuals carry a deprecation banner; they will be removed in the release after this one. Closes #242.
