/**
 * Admin API client.
 *
 * After #292, the file is empty — the only fields that lived here
 * (categories + tags admin CRUD) were dropped. Kept as a stub so any
 * future admin-only API surface has an obvious home; remove the file
 * entirely if no follow-up arrives.
 *
 * @module services/adminApi
 */

export {};
