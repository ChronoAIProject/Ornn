/**
 * Admin-related type definitions.
 *
 * After #292, the file is empty — `Tag`, `Category`, `CategoryInput`
 * went out with the admin pages they fed. Kept as a stub so any
 * future admin-only type has an obvious home.
 *
 * @module types/admin
 */

export {};
