/**
 * Admin hooks.
 *
 * After #292, the file is empty — categories + tags admin CRUD hooks
 * were dropped along with the corresponding admin pages. Kept as a
 * stub so any future admin-only hook has an obvious home; remove the
 * file entirely if no follow-up arrives.
 *
 * @module hooks/useAdmin
 */

export {};
